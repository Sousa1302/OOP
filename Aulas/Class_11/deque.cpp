#include <iostream>
#include <deque>    // double ended queue
#include <vector>
#include <queue>

using namespace std;

// functions as a dynamic array that allows fast insertion and deletion of elements at both its beginning and end

int main(){

    return 0;
}